# Claude Code — personal/local overrides (keep committed .claude/settings.json)
.claude/settings.local.json

# Flutter / Dart (for when the app is scaffolded)
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
pubspec.lock
*.iml
.idea/
.vscode/*
!.vscode/extensions.json

# OS / editor
.DS_Store
Thumbs.db

# Env / secrets
.env
.env.*
!.env.example
